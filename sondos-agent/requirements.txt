# Sondos AI — LiveKit Agent Worker Dependencies (v2 — LiveKit 1.5)

# LiveKit Agents Framework 1.5
livekit-agents[silero,openai,deepgram,elevenlabs]~=1.5.0

# aiohttp for transcript saving
aiohttp>=3.10.0

# Environment variables
python-dotenv>=1.0.0