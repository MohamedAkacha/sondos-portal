version: '3.8'

services:
  backend:
    build: ./backend
    container_name: sondos_backend
    env_file: ./backend/.env
    ports:
      - "5000:5000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: sondos_frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

  # LiveKit Voice Agent Worker (Python)
  agent:
    build: ./sondos-agent
    container_name: sondos_agent
    env_file: ./sondos-agent/.env
    restart: unless-stopped
